"STRING TEST!!!".println();
"Colon : Test!".println();

a : ":::::::";
a.println();

count_str : {
  (n) ?(n.lssr(1))-> n.str();
  (n)             -> n.str().con(count_str(n.add(-1)));
};
count_str_print : {
  (n) -> count_str(n).println();
};

for : {
  (n, f) ?(n.lssr(1))-> f(n); 
  (n, f)             -> {f(n); for(n.add(-1),f)}; 
};

for(125, count_str_print);