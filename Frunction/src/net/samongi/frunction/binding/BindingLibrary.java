package net.samongi.frunction.binding;

import java.util.HashMap;

public class BindingLibrary
{
	/**Symbol bindings is a libary of a symbol (string representation) mapped to a binding.
	 * The binding should also have the symbol associated with it.
	 */
	private HashMap<String, Binding> symbol_bindings = new HashMap<>();
	
	
}
